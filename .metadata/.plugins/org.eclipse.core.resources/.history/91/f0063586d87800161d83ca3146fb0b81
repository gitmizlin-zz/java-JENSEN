package lektionmedjdbc;

import java.sql.*;

public class JDBC {
	
	final static String CONN_STRING = "jdbc:mysql://localhost/epic_rpg";
	final static String USER = "root";
	final static String PASSWORD = "";

	public static void main(String[] args) {
		
		try  (
			Connection conn = DriverManager.getConnection
			(CONN_STRING, USER, PASSWORD);
				
			PreparedStatement preparedStmt = conn.preparedStmt
					("SELECT name FROM items WHERE id = ?");
			)	
		{
		
		
			stmt.setInt(1, 4);
			ResultSet rs = stmt.executeQuery();
			rs.next();
			String name = rs.getString("name");
			System.out.println(name);			
		}	
		} catch (SQLException e) {
			System.out.println(e.getMessage());
		}

	}

}


//Connection
//Statement
//ResultSet

//getConnection tar tre parametrar. FÖrsta är en URL-string till vår databas
//användarmn och lösenord''

//ResultSet-metoder
//next()
//previous()
//absolute(int id)
//first()
//last()
//getRow()
//isBeforeFirst()
//isAfterLast()
//isFirst()
//isLast()
//getString(String columname)
//getInt(int columnID)
